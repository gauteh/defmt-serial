[package]
name = "example-std"
version = "0.1.0"
edition = "2021"

[dependencies]
embedded-io = "0.7"
defmt = "1.1.1"
defmt-serial = { path = "../" }
nb = "1.0.0"
critical-section = { version = "1.1", features = ["std"]}
static_cell = "2.0.0"
